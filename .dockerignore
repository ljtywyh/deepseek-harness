# 依赖 / 构建缓存 —— 不进入构建上下文
# 注意：不要排除 .git！pnpm run build 会调用 `git rev-parse HEAD` 做版本戳，
# 若 .git 不在构建上下文中会导致构建失败。
.gitignore
**/node_modules
**/.turbo
coverage
**/*.log
.DS_Store

# 宿主机可能残留的构建产物由镜像内重新生成，避免带入陈旧文件
**/lib
**/dist

# 测试快照 / 大体积非运行必需目录（按需保留也可删除下面这行）
scripts/snapshots
